# Serves the drill's synthetic metric fixture as a Prometheus scrape target.
# Prometheus needs text/plain; nginx would otherwise guess from the extension.
server {
    listen 9101;
    default_type text/plain;
    location /metrics {
        alias /fixtures/metrics.txt;
        add_header Content-Type "text/plain; version=0.0.4" always;
    }
}
